
function WORDchoice(){
  noLoop();
  let rando = floor(random(0,WORD.length + 1));
  FirstWORD = WORD[rando];
  console.log(FirstWORD);
  
  
  FirstLength = FirstWORD.length;
  console.log(FirstLength);
  
  
  
}

